Done:

1) YAML Parser
2) Bash helper (call .sh scripts or execute commands)

Todo:

3) GIT Hooks
4) Base Web App (receive events & display results)
5) Database (build requests, history, events)
6) Clone repositry (Github API, OAuth2)
7) Container support (Docker)